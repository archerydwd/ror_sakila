<%= form_for(@film_actor) do |f| %>
  <% if @film_actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@film_actor.errors.count, "error") %> prohibited this film_actor from being saved:</h2>

      <ul>
      <% @film_actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :actor_id %><br>
    <%= f.text_field :actor_id %>
  </div>
  <div class="field">
    <%= f.label :film_id %><br>
    <%= f.text_field :film_id %>
  </div>
  <div class="field">
    <%= f.label :last_update %><br>
    <%= f.datetime_select :last_update %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
